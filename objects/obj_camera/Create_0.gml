max_width = 1920;
max_height = 1080;

min_width = max_width/4;
min_height = max_height/4;

cam_width = camera_get_view_width(view_camera[0]);
cam_height = camera_get_view_height(view_camera[0]);

width_to = camera_get_view_width(view_camera[0]);
height_to = camera_get_view_height(view_camera[0]);

follow = obj_player;

x_to = x;
y_to = y;


